# Terraform Azure Environment Variables
# Copy this file to .env and update with your actual values
# Then source it before running Terraform commands

# Azure Subscription ID (required)
# Get this from: az account show --query id -o tsv
ARM_SUBSCRIPTION_ID="e9562a9b-8aa6-4733-9926-1c778880511b"

# Azure Tenant ID (optional - uses Azure CLI if not set)
# Get this from: az account show --query tenantId -o tsv
ARM_TENANT_ID="aab954a2-91da-406b-aa78-78ca94176e8e"

# For Service Principal authentication (optional - leave blank to use Azure CLI)
# ARM_CLIENT_ID="your-service-principal-client-id"
# ARM_CLIENT_SECRET="your-service-principal-secret"

# Alternative: Use Azure CLI authentication (recommended for local development)
# Just run 'az login' and these variables are optional

# To use this file:
# PowerShell:
#   Get-Content .env | ForEach-Object { if($_ -match "^([^#][^=]+)=(.*)$") { [Environment]::SetEnvironmentVariable($matches[1], $matches[2]) } }
# 
# Bash/WSL:
#   export $(grep -v '^#' .env | xargs)
#
# Or set them individually in PowerShell:
#   $env:ARM_SUBSCRIPTION_ID="e9562a9b-8aa6-4733-9926-1c778880511b"
#   $env:ARM_TENANT_ID="aab954a2-91da-406b-aa78-78ca94176e8e"